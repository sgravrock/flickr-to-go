This is a tool that backs up your Flickr account. It's incomplete and won't
be finished. Use https://github.com/sgravrock/flickr-to-go-go instead.

# Initial setup

1. Install virtualenv, if it's not already solved.
2. $ virtualenv venv
    * NOTE: If the default python is Python 3, run this instead:
      $ virtualenv --python=`which python2.7` venv
3. $ source venv/bin/activate
4. $ pip install -r requirements.txt

# Running the tests

$ python -m unittest discover
